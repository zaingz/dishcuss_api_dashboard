<!-- Content Header (Page header) -->
<section class="content-header">
  <h1>
    Post
    <small></small>
  </h1>
</section>

<div class="row content">
  <div class="col-xs-12">
    <div class="box">
            <!-- <div class="box-header">
              <h3 class="box-title">Reviews</h3>
            </div> -->
            <!-- /.box-header -->
            <div class="box-body">
              <table id="example1" class="table table-bordered table-striped">
                <thead>
                  <tr>
                    <th>Title</th>
                    <th>Status</th>
                    <th>Checkin</th>
                    <th>User</th>
                    <th>Time</th>
                  </tr>
                </thead>
                <tbody>
                  <% @post.each do |us| %>
                    <tr>
                      <td><%= us.title %></td>
                      <td><%= us.status %></td>
                      <td><%= us.checkin.present? ? us.checkin.address : 'No' %></td>
                      <td><%= us.user.email %></td>
                      <td><%= us.created_at.strftime('%H:%M %F') %></td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
            <!-- /.box-body -->
          </div>
          <!-- /.box -->
  </div>
</div>